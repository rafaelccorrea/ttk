# Supabase (Settings > API do projeto). Vazio = modo demo (dev-login no backend).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Convite do grupo de WhatsApp da comunidade (aparece no fim do menu lateral).
# Vazio = o item não aparece. Link de convite expira: quando trocar o grupo,
# troque aqui e refaça o build do frontend.
VITE_WHATSAPP_GROUP_URL=
