.history/
cc-session.json
shop-session.json

# Logs de execução local
*.log

# Segredos — nunca commitar
.env
.env.*
!.env.example
*.supabase-backup

# Pacote gerado para subir o front a mao (artefato de build, nao versionado).
frontend/pikpok-front.zip
